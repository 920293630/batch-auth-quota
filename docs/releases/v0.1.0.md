# batch-auth-quota v0.1.0

首个公开版本发布，提供独立的批量认证文件额度检查工具。

## ✨ 主要功能

- 批量检查认证文件额度
- 支持 `.env`、环境变量、配置文件、命令行多层配置
- 支持 8 档剩余额度分类统计
- 支持剩余额度总量总览
- 支持 401 失效账号一键删除
- 支持额度耗尽账号一键隔离
- 支持隔离账号复查后恢复回主目录
- 支持输出汇总结果、分类清单与最新索引

## 📦 仓库信息

- 项目地址：`https://github.com/920293630/batch-auth-quota`
- 版本标签：`v0.1.0`
- 开源协议：`MIT`

## 🚀 快速开始

```bash
cd ~/batch-auth-quota
cp .env.example .env
bash run.sh --type codex
```

或安装后使用：

```bash
cd ~/batch-auth-quota
python3 -m pip install -e . --no-deps
batch-auth-quota --type codex
```

## 📁 默认路径

- 配置文件：`~/.batch-auth-quota/config.json`
- 输出目录：`~/.batch-auth-quota/results/latest`
- 默认认证目录：`~/.cli-proxy-api`
- 默认隔离目录：`~/.cli-proxy-api/.quota_isolated`

## 📝 说明

这是首个初始化公开版本，当前重点是把批量额度检查能力独立出来，便于单独维护、分发与使用。

后续可继续演进：

- 更完整的安装分发方式
- 自动化测试与 CI 校验
- 更多统计口径与输出视图
- 更细致的异常诊断与恢复流程
